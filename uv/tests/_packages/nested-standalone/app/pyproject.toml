[project]
name = "app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "lib-pkg",
]

[dependency-groups]
dev = ["six"]

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
lib-pkg = { path = "../my-lib", editable = true }
