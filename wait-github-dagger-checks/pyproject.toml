[project]
name = "wait-github-dagger-checks"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["dagger-io", "httpx>=0.27", "rich>=13"]

[build-system]
requires = ["uv_build>=0.8.4,<0.9.0"]
build-backend = "uv_build"

[tool.uv.sources]
dagger-io = { path = "sdk", editable = true }
